#include <iostream>

float f2c(float f);

int main()
{
    float f = 93;
    float c = 0;

    c = f2c(f);
    std::cout << c << std::endl;
    return 0;
}

float f2c(float f)
{
    float c;

    c = (f - 32) * 5.0 / 9.0;
    return c;
}